{
  "name": "yuti",
  "version": "1.5.0",
  "description": "Yuti - Natural Language Crypto Hub with AI Assistant",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "sh -c 'git clone --depth 1 -b stable https://github.com/flutter/flutter.git /tmp/flutter && export PATH=/tmp/flutter/bin:$PATH && flutter config --enable-web && flutter --version && flutter pub get && flutter build web --release --pwa-strategy offline-first'",
    "start": "echo 'Static files ready'",
    "test": "echo 'No tests configured'"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [
    "yuti",
    "crypto",
    "blockchain",
    "AI",
    "DeFi",
    "trading",
    "Web3",
    "natural-language",
    "conversation",
    "agent-t"
  ],
  "author": "Flux Point Studios",
  "license": "MIT",
  "devDependencies": {
    "supabase": "^2.31.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.0",
    "pg": "^8.12.0"
  }
}
